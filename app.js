const menu= document.querySelector('#moile-menu')
constmenuLinks=documents.querySelector('.navbar__menu')

menu.addEventListener ('click',function(){
    menu.classList.toogle('is-active');
    menuLinks,classList.toggle('active');
});